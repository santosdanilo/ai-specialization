[project]
name = "legal-project-app"
version = "0.1.0"
description = "Private legal AI application"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
